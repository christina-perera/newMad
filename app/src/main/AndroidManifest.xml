<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thefeast">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FeedBackManagement.ViewFeedBack"></activity>
        <activity
            android:name=".FeedBackManagement.MainFeedBack"
            android:label="@string/title_activity_main_feed_back"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".FeedBackManagement.AddFeedBack" />
        <activity
            android:name=".AdminPanel"
            android:label="@string/title_activity_admin_panel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUp" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".ViewMain2Activity" />
        <activity android:name=".BeverageManagement.MainBActivity" />
        <activity android:name=".BeverageManagement.ViewBActivity" />
        <activity android:name=".Login" />

        <activity android:name=".MainActivity" />

        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>